# Serving dependencies only. Everything heavy (torch, faiss, pandas, scipy) is
# build-time and lives in pipelines/requirements-build.txt, so the deployed
# function stays small and cold-starts fast.
fastapi==0.118.0
pydantic==2.11.7
numpy==2.2.6
